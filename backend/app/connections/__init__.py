"""
Kernel neutro de conexões (`E7.4-1`).

```text
ENUM_OR_REGISTRY != AVAILABLE
FAMÍLIA != RELEASE != PROVEDOR_DE_ACESSO != CONEXÃO
KERNEL_NÃO_IMPORTA_MCP · SEM REDE · SEM CREDENCIAL
```

Método de conexão é **dado**, não subclasse: o kernel descreve o que
existe, com que estado e com que capacidade observada, e não transporta
nada. Transporte é da orquestração; adaptador é entrega própria.
"""
