# =============================================================================
# Overlay — ambiente de testes (Módulo 2.11)
# Combinar com backend/.env.example — não é um arquivo de configuração completo.
# =============================================================================

ENVIRONMENT=testing
DEBUG=false

# Placeholder reconhecível — nunca um segredo real.
SECRET_KEY=test-secret-key-not-for-production
DB_PASSWORD=test-secret-key-not-for-production

LOG_LEVEL=WARNING
LOG_FORMAT=json

TRUSTED_HOSTS=*
CORS_ALLOWED_ORIGINS=*
